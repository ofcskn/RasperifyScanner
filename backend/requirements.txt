fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic[email]==2.13.4
pydantic-settings==2.14.1
sqlalchemy[asyncio]==2.0.50
aiosqlite==0.20.0
google-genai==2.7.0
openai==1.109.1
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
APScheduler==3.10.4
python-multipart==0.0.9
websockets==16.0
python-dotenv==1.0.1
pyyaml==6.0.1
httpx==0.28.1
Pillow==11.3.0
# picamera2 and libcamera are Pi OS system packages (not on PyPI for arm64).
# On the Pi they are bind-mounted from the host into the container via
# docker-compose volumes; on macOS/Windows dev the mock camera is used instead.
# --- Stage 1 local detection ---
onnxruntime==1.20.1
numpy==2.2.1
opencv-python-headless==4.10.0.84
